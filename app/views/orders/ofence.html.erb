<div class="order-main-image">
  <div class="top-session-wrap">

    <%= render "shared/header" %>
    <div class="main-ofence-contents-wrap">
      <div class="ofence-contents-wrap">
        <div class="ofence-contents">
          <div class="ofence-data-wrap">
            <div class="ofence-search-wrap">
            <%= search_form_for @p, url: search_orders_path do |f| %>
              <%= f.label :match_name_eq, '対戦相手', class: "search-input" %>
              <%= f.collection_select :match_name_eq, @match_names.where(user_id: current_user.id), :match_name, :match_name,  include_blank: '指定なし' %>
              <%= f.label :year_id_eq, '対戦相手', class: "search-input" %>              
              <%= f.collection_select :year_id_eq, Year.all, :id, :name,  include_blank: '指定なし' %>
              <%= f.label :month_id_eq, '対戦相手', class: "search-input" %>
              <%= f.collection_select :month_id_eq, Month.all, :id, :name,  include_blank: '指定なし' %>
              <%= f.label :day_id_eq, '対戦相手', class: "search-input" %>
              <%= f.collection_select :day_id_eq, Day.all, :id, :name,  include_blank: '指定なし' %>
              <%= f.submit "検索", class: "search-btn" %>
            <% end %>
            </div>
          </div>
        </div>

        <div class="ofence-contents">
          <div class="ofence-history-wrap">
            <% @matchs.each do |match| %>
              <% if current_user.id == match.user_id %>
              <div class="ofence-history">
                <%= link_to order_path(match.id) do %>
                <div class="match-name-wrap">
                  <p class="ofence-user-name"><%= current_user.name %></p>
                  <label class="ofence-form-text"> VS </label>
                  <p class="ofence-match-name"><%= match.match_name %></p>
                </div>
                <div class="match-date-wrap">
                  <p class="ofence-match-year"><%= match.year.name %>年</p>
                  <p class="ofence-match-month"><%= match.month.name %>月</p>
                  <p class="ofence-match-day"><%= match.day.name %>日</p>
                </div>
                <% end %>
              </div>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </div>


  </div>
</div>
<%= render "shared/footer" %>